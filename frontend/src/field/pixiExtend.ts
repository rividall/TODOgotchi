import { extend } from "@pixi/react";
import { AnimatedSprite, Container, Graphics, Sprite, TilingSprite } from "pixi.js";

// Must be called once before any <pixi*/> JSX is used.
extend({ AnimatedSprite, Container, Graphics, Sprite, TilingSprite });
